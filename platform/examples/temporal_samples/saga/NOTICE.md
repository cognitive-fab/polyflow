# Notice

`src/activities.ts`, `src/clients/index.ts` and `src/types/*.ts` are byte for
byte, and `src/client.mjs` derives from, temporalio/samples-typescript (`saga`,
commit 8907f29), Copyright (c) 2022 Temporal Technologies Inc., under the MIT
License. The licence text and the pinned copies are in
`platform/examples/upstream/samples-typescript@8907f29/`. Which files are
byte-identical and which changed, and why, is in `upstream.json`.
